<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat theme because MainActivity is an AppCompatActivity (per-app language). Compose draws everything else. -->
    <style name="Theme.OpenFuel" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
